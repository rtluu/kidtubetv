[build]
  publish = "dist"
  command = "expo export -p web"

[functions]
  directory = "netlify/functions"

[dev]
  command = "npx expo start --web"
  targetPort = 8081
  port = 8888
  autoLaunch = false
